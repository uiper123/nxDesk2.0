import React from "react";

describe("Settings Component", () => {
    it("should toggle acceleration config successfully", () => {
        const acceleration = true;
        expect(acceleration).toBe(true);
    });
});
