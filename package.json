{
  "name": "ttgtiso-desk",
  "private": true,
  "workspaces": [
    "apps/desktop-client",
    "packages/ui",
    "packages/shared-types"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/desktop-client",
    "build": "npm run build --workspace=apps/desktop-client",
    "tauri": "npm run tauri --workspace=apps/desktop-client",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
